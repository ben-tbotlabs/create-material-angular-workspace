import { createFeatureSelector, createSelector } from '@ngrx/store';

import { LAYOUT_FEATURE_KEY, State } from './layout.reducer';

export const selectLayoutState = createFeatureSelector<State>(
	LAYOUT_FEATURE_KEY
);

export const selectIsSidenavOpen = createSelector( selectLayoutState, ( state: State ) => state.sideNavOpened );
